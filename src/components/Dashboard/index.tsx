import React from 'react'

import {Container} from './styles'

interface Props {
    
}

export const Dashboard = (props: Props) => {
    return (
        <Container>
            
        </Container>
    )
}
